\documentclass{minimal}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{tikz}

\begin{document}
	\pagecolor{yellow}
	\usetikzlibrary{trees}
	\tikzstyle{every node}=[draw=red, red,thick,anchor=west]
	\tikzstyle{every child}=[draw=red,thick,anchor=west]
	\tikzstyle{selected}=[draw=red,fill=red!30]
	\tikzstyle{selectedg}=[draw=green,fill=green!30]
	\tikzstyle{selectedo}=[draw=orange,fill=orange!30]
	\tikzstyle{selectedb}=[draw=blue,fill=blue!30]
	\tikzstyle{optional}=[dashed,fill=gray!50]
	\begin{tikzpicture}[%
	grow via three points={one child at (0.5,-0.7) and
		two children at (0.5,-0.7) and (0.5,-1.4)},
	edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
	\node {App}	
	child { node {Student System}
		child { node {Static part}
			child { node {Section "Courses"}}
			child { node {Section "Video lessons"}}
			child { node {Section "Presentations"}}
		}
		child [missing] {}				
		child [missing] {}				
		child [missing] {}
		child { node {DataBases}}
		child { node {Certificates}}
	}
	child [missing] {}				
	child [missing] {}				
	child [missing] {}
	child [missing] {}				
	child [missing] {}				
	child [missing] {}
	child { node {Testing system - The Jugdata}
		child { node {DataBases}}
		child { node {Appearence}}
	}
	child [missing] {}				
	child [missing] {}	
	child { node {Chat/Forum}
		child { node {DataBases}}
		child { node {Rooms}}
	}
	;
	\end{tikzpicture}
		\begin{tikzpicture}[%
	grow via three points={one child at (0.5,-0.7) and
		two children at (0.5,-0.7) and (0.5,-1.4)},
	edge from parent path={(\tikzparentnode.south) |- (\tikzchildnode.west)}]
	\node[selected] {NGINX server}	
	child { node {App}
		child { node {Student System}
			child { node {Static part}
				child {
					node[selectedo] {Bootstrap (Jumbotron)}
					child { node {Section "Courses"}}
					child { node {Section "Video lessons"}}
				}
				child [missing] {}				
				child [missing] {}	
				child { 
					node[selected] {Reveal.JS}
					child {
						node {Section "Presentations"}
					}
				}
			}
			child [missing] {}	
			child [missing] {}				
			child [missing] {}				
			child [missing] {}				
			child [missing] {}	
			child { node {DataBases}
				child { node[selected] {MySQL}}
			}			
			child [missing] {}	
			child { node {Certificates}}
		}
		child [missing] {}			
		child [missing] {}					
		child [missing] {}	
		child [missing] {}				
		child [missing] {}				
		child [missing] {}
		child [missing] {}				
		child [missing] {}				
		child [missing] {}
		child { node {Testing system - The Jugdata}
			child { node {DataBases}
				child { node[selectedg] {MongoDB}}
			}
			child [missing] {}
			child { node {Appearence}
				child { node[selectedb] {JADE}}
			}
		}
		child [missing] {}				
		child [missing] {}
		child [missing] {}				
		child [missing] {}	
		child { node {Chat/Forum}
			child { node[selectedg] {Node.JS}
				child { node {DataBases}
					child { node[selectedg] {MongoDB}}
				}			
				child [missing] {}	
				child { node {Room}}
			}
		}
	}
	;
	\end{tikzpicture}
\end{document}